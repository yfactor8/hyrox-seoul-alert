# Runtime artifacts — not part of the repo
state.json
monitor.log
__pycache__/
*.pyc

# Local overlay holding the real ntfy topic — never commit this
config.local.json

# Local Claude session data
.claude/
